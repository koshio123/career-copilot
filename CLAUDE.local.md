# 転職活動支援Webアプリ — プロジェクト仕様

このドキュメントは、Claudeとの設計検討の内容をまとめたものです。ローカル開発環境（Claude Code等）にプロジェクトコンテキストとして読み込ませる用途を想定しています。

## 1. プロダクト概要

転職活動を支援するWebアプリケーション。主な価値提供：

- 求人情報の自動取得
- 職務経歴と求人票のギャップ分析（不足経験のアドバイス）
- レジュメの自動添削・求人への適応（求人ごとのカスタマイズ）

開発者本人（バックエンドエンジニア、Python/FastAPI/PostgreSQL/AWS）自身の転職活動での実用も視野に入れている。

## 2. システムアーキテクチャ

```
[フロントエンド]
      ↓
[バックエンドAPI（FastAPI）] — 認証・リクエスト制御・オーケストレーション
      ↓
[非同期ワーカー] — 求人取得・レジュメ分析
   ↑                        ↑
[求人サイト/登録URL]    [外部LLM API]
      ↓
[PostgreSQL]
```

- **フロントエンド**：Next.js等のSPA。求人一覧・分析結果・添削結果の表示が中心
- **バックエンドAPI**：FastAPI。同期的な処理（認証、CRUD、検索）を担当
- **非同期ワーカー**：時間のかかる処理（求人取得・LLM分析）をキュー化。SQS + Lambda または Celery + SQS
  - 求人取得：定期実行（EventBridge）
  - レジュメ分析：ユーザー操作をトリガーにキュー投入
- **DB**：PostgreSQL。求人情報、ユーザーのスキル/経験、分析結果の履歴を保持

### 2.1 インフラ

インフラは **AWS** を前提とする（開発者本人がAWSに習熟しているため）。想定するサービス構成：

| レイヤー | 想定サービス |
|---|---|
| フロントエンド配信 | S3 + CloudFront（または Amplify Hosting / Vercel） |
| バックエンドAPI | ECS Fargate または Lambda + API Gateway |
| 非同期ワーカー | SQS + Lambda（または Celery + SQS を ECS/Fargate 上で実行） |
| 定期実行 | EventBridge Scheduler |
| DB | RDS for PostgreSQL（Aurora Serverless v2 も選択肢） |
| オブジェクトストレージ | S3（レジュメPDF/Word、取得HTMLの生データ） |
| シークレット管理 | Secrets Manager / SSM Parameter Store（LLM APIキー等） |
| 認証 | Cognito（またはアプリ実装 + RDS） |
| 監視・ログ | CloudWatch Logs / Metrics、X-Ray |
| IaC | Terraform または AWS CDK |

## 3. 機能一覧（カテゴリ別）

### 3.1 ユーザー管理・オンボーディング
- アカウント登録/認証（メール、Google/LinkedIn連携）
- 初期レジュメ登録（PDF/Wordアップロード or フォーム入力→LLMで構造化）
- スキル・経験のタグ付け（自己申告＋レジュメからの自動抽出の突き合わせ）
- 希望条件の登録（職種、年収レンジ、勤務地、リモート可否、転職時期）

### 3.2 レジュメ・職務経歴管理
- 職務経歴書のバージョン管理（求人ごとのカスタマイズ版を保存・比較）
- 経歴の構造化データ化（会社/期間/役割/実績を項目化）
- 実績の定量化サポート（数値化の指摘）
- エクスポート（Word/PDF、企業ごとのフォーマット対応）

### 3.3 求人管理
- 求人情報の取得・登録（自動取得）
- 保存・ブックマーク、ステータス管理
- 求人票の構造化（LLMで必須/歓迎スキル・年収・勤務地等をタグ化）
- 重複求人の名寄せ

### 3.4 マッチング・分析
- 経歴×求人票のマッチ度スコアリング
- 不足スキル・経験のギャップ分析
- 学習/資格取得の推奨
- レジュメの求人適応（自動添削・カスタマイズ）

### 3.5 応募・選考プロセス管理
- 応募進捗のカンバン管理（書類選考中/一次面接/最終面接/内定/辞退）
- 選考スケジュール管理・リマインダー通知
- 企業ごとのメモ・面接フィードバック記録
- 複数内定時の比較検討サポート

### 3.6 面接対策
- 想定質問の自動生成
- 模擬面接（LLM対話形式）
- 企業研究サマリー

### 3.7 通知・レポート
- 新着求人のマッチ度アラート
- 選考ステータス変化のリマインダー
- 進捗ダッシュボード（応募数、通過率、平均選考期間）

### 3.8 非機能・運用系
- 個人情報の暗号化・アクセス制御（職務経歴は機微データ）
- LLM利用のコスト管理・レート制限
- 求人データソースの利用規約遵守状況の管理
- 分析結果の説明可能性

### MVPスコープ（推奨順）
1. 初期レジュメ登録
2. 求人登録（手動 or URL登録）
3. ギャップ分析・レジュメ添削
4. 選考進捗管理（簡易版）

面接対策（3.6）は独立性が高く後回し可。

## 4. 求人取得の自動化方針

### 4.1 検討した選択肢と結論

| 方法 | 状況 | 採否 |
|---|---|---|
| ハローワークAPI（公式） | 厚労省提供、求人事業主が同意した求人情報をXML形式で取得可能。無料。ただし利用申請に審査あり | 不採用 |
| Indeed Publisher API | 2023年に廃止済み。現在は自己登録APIキーなし。エンタープライズ契約かiframe埋め込みのみ | 不採用 |
| Adzuna / Jooble / Talent.com等 | パートナー/publisherプログラムを運用中とされるが条件は要確認・変動あり | 将来検討 |
| 独自スクレイピング（大手媒体） | 利用規約違反リスク・法的リスクが高く、構造変更に弱い | 不採用 |
| **schema.org/JobPosting の JSON-LD 抽出** | Google しごと検索対応で多くの採用ページ・媒体が `<script type="application/ld+json">` に `JobPosting` 型を埋め込んでいる。規格化フィールドをローカル処理で取得（トークン消費ゼロ、ハルシネーションなし） | **採用** |
| **ATS 公開ジョブボード API** | Greenhouse / Lever / Ashby / Workable / SmartRecruiters / Recruitee / Workday 等が認証不要の JSON API を求人配信目的で公開。採用ページから ATS を判定し該当 API を叩く | **採用** |
| **ユーザー登録URL＋LLM抽出** | 上記いずれも取れない採用ページの汎用クロール＋LLM構造化。フォールバック経路 | **採用（フォールバック）** |

方針：大手求人媒体の大規模スクレイピングは避け、ユーザーが登録した企業の採用ページを次の優先順位で取り込むハイブリッド構成とする。(1) ATS 判定できれば公開 API、(2) ページに `JobPosting` の JSON-LD があればそれを構造化データとして採用、(3) どちらも無ければ従来の trafilatura + LLM 構造化にフォールバック。(1)(2) に載る割合が増えるほど LLM コストとハルシネーションが減る。

### 4.2 URL登録＋求人取得パイプライン

処理フロー：
1. ユーザーがURL登録（採用サイトトップ or 求人一覧ページ）
2. **ソース判定**：登録URL（ホスト・パス）と初回取得HTMLから経路を決定
   - **A. ATS 判定**：URL パターンや埋め込みスクリプト/リンクから ATS（Greenhouse / Lever / Ashby / Workable / SmartRecruiters / Recruitee / Workday 等）と board 識別子を特定 → 該当の公開 JSON API から求人一覧＋詳細を直接取得。手順3〜4・7をスキップし手順5へ
   - **B. JSON-LD 経路**：ATS 不明だがページに `JobPosting` 型の JSON-LD がある → 一覧/詳細ページの JSON-LD をパースして構造化データを取得。手順7をスキップ。欠損フィールドは「要確認」フラグ
   - **C. フォールバック**：いずれも無ければ従来のクロール＋LLM抽出（手順3〜7）
3. クロール（C のみ）：登録URLから求人詳細ページへのリンクを抽出
4. 本文取得・本文抽出（C のみ）：各詳細ページのHTML取得 → ナビゲーション/フッターを除去し本文のみ抽出（trafilatura等）
5. 差分検知：正規化後テキスト（A/B は構造化データを正規化した文字列、C は抽出本文）のハッシュ値を前回と比較し、一致すれば以降をスキップ
6. 一次フィルタ（ルールベース）：テキスト/構造化データに対しキーワード/正規表現で希望条件と明らかに不一致な求人を除外（例：職種カテゴリが全く異なる、勤務地NG かつリモート不可、雇用形態が対象外）。ここで除外したものはLLMを呼ばない
7. LLMによる構造化（C のみ）：差分あり かつ 一次フィルタ通過の場合のみ、JSON Schema指定でClaude APIに抽出依頼（職種名/必須スキル/年収/勤務地等）
8. マッチ：構造化データを希望条件と照合し、マッチ度スコアを算出
9. 二次フィルタ（スコア閾値）：マッチ度スコアが閾値未満の求人は保存せず破棄する

A/B/C いずれの経路でも最終的な構造化データのスキーマは共通とし、`source_type`（`ats` / `json_ld` / `llm`）と、A の場合は `ats_vendor` を保持する。ATS 公開 API はベンダごとにレスポンス形状・ページネーション・日付形式が異なるため、ベンダ別アダプタ→共通スキーマへの正規化レイヤーを設ける。

差分検知は必ずLLM呼び出し（手順7）の前に置く。経路 C のハッシュ対象は抽出後の本文テキストとする（生HTMLは広告・トークン・タイムスタンプ等で毎回変わり偽陽性が多いため）。本文抽出（trafilatura等）はローカル処理でトークン消費ゼロなので、抽出をゲートより前に置いてよい。任意で、生HTMLハッシュを一次ゲート（一致時は抽出もスキップ）にする二段構成も可。経路 A/B は取得済みの構造化データを正規のフィールド順で文字列化したものをハッシュ対象とする。

フィルタは2段階：手順6のルールベース一次フィルタでLLMコストを抑え、手順9のスコア閾値による二次フィルタで保存対象を絞る。一次フィルタは誤除外を避けるため「明らかな不一致」のみを対象とし、判断が微妙なものは通過させてLLM構造化に回す。除外した求人はDBに残さない（希望条件・閾値を変更した場合は次回クロール時に再取得・再評価される）。

技術的な課題と対策：

| 課題 | 対策 |
|---|---|
| ATS 判定の精度 | URL パターン（`boards.greenhouse.io`, `jobs.lever.co`, `*.ashbyhq.com` 等）＋ページ内のスクリプト/iframe/リンク先ホストで判定。iframe 埋め込み型は親ページの埋め込み先から board 識別子を抽出 |
| ATS API のベンダ差 | ベンダ別アダプタで共通スキーマへ正規化。未対応ベンダは経路 C にフォールバック |
| JSON-LD の項目欠損・表記ゆれ | 給与等の任意項目は未記載が多い前提。必須フィールド欠如時は「要確認」フラグ。`@graph` 形式・複数 `JobPosting`・HTML エンティティに対応 |
| JSレンダリングサイト | Playwrightでレンダリング後のHTML取得（経路 C）。ATS 公開 API・JSON-LD は静的取得で足りることが多い |
| 一覧→詳細リンク発見 | キーワード（求人/recruit/job等）で候補抽出→LLMで「求人詳細ページか」を判定する二段構え |
| LLMコスト | trafilatura等で本文抽出しHTMLノイズを除去してからLLMに渡す。ハッシュ比較で不要な再解析を回避 |
| robots.txt・利用規約 | クロール前にrobots.txtを機械的にチェック。Disallow対象は取得しない。アクセス間隔を制御 |
| 誤抽出 | JSON Schemaで出力形式を強制。必須フィールド欠如時は「要確認」フラグを付与 |
| 取得失敗時の挙動 | サイレントリトライを繰り返さず、ユーザーに取得失敗を明示し手動登録に誘導 |
| フィルタの誤除外 | 一次フィルタは「明らかな不一致」のみ対象。判断が微妙なものは通過させLLM構造化に回す。閾値は緩めから運用開始し、取りこぼしを見ながら調整 |

想定スタック：
- 取得：`httpx`（静的・ATS API・JSON-LD）/ `playwright`（JS必須サイト、経路 C）
- ATS アダプタ：ベンダ別クライアント（Greenhouse `boards-api.greenhouse.io/v1/boards/{token}/jobs?content=true`、Lever `api.lever.co/v0/postings/{company}?mode=json`、Ashby `api.ashbyhq.com/posting-api/job-board/{company}` 等）→ 共通スキーマへ正規化
- JSON-LD 抽出：`extruct` または `lxml` で `<script type="application/ld+json">` をパースし `JobPosting` を抽出
- 本文抽出（経路 C）：`trafilatura` または `readability-lxml`
- 構造化（経路 C）：Claude APIにJSON Schema付きプロンプト
- スケジューリング：登録URLごとにEventBridge + Lambda、または既存SQSワーカー
- 保存：閾値を超えた求人のみ、構造化結果 + `source_type` + `ats_vendor` + `raw_text_hash` + マッチ度スコア をPostgreSQLへ

## 5. 未決定・要検討事項

- DBスキーマ設計（複数ソース〔`ats` / `json_ld` / `llm`〕の求人を名寄せできる構造）
- 対応する ATS ベンダの優先順位（日本の利用実績が多い Greenhouse / Lever / HERP から着手するか）と国産 ATS（HERP / HRMOS / Talentio）の公開 API 有無の調査
- ATS 公開 API・JSON-LD 取得時の利用規約／robots.txt の扱い（公開配信目的の API でも各社規約を確認）
- ギャップ分析・レジュメ添削のプロンプト設計（ハルシネーション対策、Before/After差分表示UI）
- Adzuna等アグリゲーターAPIの日本国内での提供状況の確認
